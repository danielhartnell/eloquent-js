// Summary: The while loop will execute the block while the condition is true.
// Output should be 55
var total = 0, count = 1;
while (count <= 10) {
	total += count;
	count += 1;
}
console.log(total);

// FlashcardQ1: What is code surrounded by { and } called?
// FlashcardA1: The block

// FlashcardQ2: What does the += assignment operator do?
// FlashcardQ2: x += y would be the same as x = x + y
